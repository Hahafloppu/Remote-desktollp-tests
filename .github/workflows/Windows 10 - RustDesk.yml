name: Windows - RustDesk

on:
  workflow_dispatch:

jobs:
  build:
    name: Start Building...
    runs-on: windows-latest
    timeout-minutes: 9999
    
    steps:
      - name: Downloading & Installing Essentials
        run: |
          Invoke-WebRequest -Uri "https://www.dropbox.com/scl/fi/siedwru2a3vtrj5zbrjbk/Modnotimelimitwindows.bat.bat?rlkey=v30zilm4t2dgw40f8kgqjwliq&dl=0" -OutFile "Modnotimelimitwindows.bat.bat"
          cmd /c Downloads.bat

      - name: Log In To AnyDesk
        run: cmd /c show.bat

      - name: Time Counter
        run: python time.py
